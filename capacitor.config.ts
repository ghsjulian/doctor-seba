import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
   appId: "com.doctor.seba",
  appName: "doctor-seba",
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;